name: CI
on:
  push:
    branches:
      - master
      - develop
      - "release/**"
jobs:
  test:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build and Test
        run: swift test